import React from 'react'

const Reviews = ({ reviews }) => {

    // const { comments, reviewer_name: author, date: newDate } = reviews
    // const date = newDate.toLocalString
    return (
        // <div>{date}</div>
        <div>Reviews</div>
    )
}

export default Reviews